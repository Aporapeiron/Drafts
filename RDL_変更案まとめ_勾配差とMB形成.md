# RDL 変更案まとめ
## 勾配差・変化履歴・M_B形成に関する再整理案

*Draft / discussion summary*

---

## 0. この文書の位置づけ

本書は、RDL T0 基底仮設（HYP）および T0 最低動作仕様（SPEC）を前提としつつ、
以下の議論から生じた変更候補を整理するための暫定文書である。

- 同一の更新前 `M_B` で `F` と `F'` を形成することへの違和感
- 相互作用そのものが `M_B` を変化させる可能性
- `M_B` よりも変化量・勾配差を一次的に扱う可能性
- 初期には未分化な `ξ` しかなく、変化履歴から `M_B` が析出する可能性
- `M_B` を「生き残った履歴」「残存した関係構造」として再解釈する可能性
- T1 基本代謝との対応

本書は現時点では**変更確定案ではなく、再設計候補の整理**である。

---

## 1. 現行T0に対する主要な違和感

現行SPECでは、概ね次の形をとる。

```text
F(t)    = interp(M_B, EFP(t))
F'(t+Δ) = interp(M_B, EFP(t+Δ))
E       = Δ(F, F')
```

ここでは比較条件を揃えるため、`F` と `F'` の双方を**同一の更新前 `M_B`**から形成する。

しかし、相互作用を考えると、

```text
EFP → M_B
```

という作用が発生した時点で、`M_B` 自体も微小に変化している可能性が高い。

したがって実際の系では、

```text
M_B(t) ≠ M_B(t+Δ)
```

であることが自然であり、

> 一回目の作用を受けても変化しない `M_B` が、二回目の作用を解釈する

という構図は、比較器としては便利でも、実際の力学としては凍結しすぎている可能性がある。

---

## 2. 人間の視覚系を使った検討

人間の目では、入力を受けること自体が視覚系の状態を変える。

例：

- 明所から暗所への移動
- 瞳孔径の変化
- 網膜感度の変化
- 視細胞の順応
- 残像
- 後段視覚処理の利得変化

この場合、

```text
EFP_0 → F_0
```

だけでなく、同時に

```text
EFP_0 → δM_B
```

も起きていると考える方が自然である。

したがって、次の入力 `EFP_1` は、

```text
F_1 = interp(M_1, EFP_1)
```

として形成される。

ここで

```text
M_1 = M_0 + δM
```

である。

この観点では、相互作用は「解釈を出す処理」ではなく、同時に「自己構造を変える処理」でもある。

---

## 3. 固定 `M_B` は実状態ではなく比較器として残す

同一 `M_B` を完全に捨てる必要はない。

むしろ、固定 `M_B` は**反実仮想的な比較器**として残すとよい。

```text
M_0 = M_B(t)
```

現在構造から予測を形成する。

```text
F_hat = Pred(M_0, EFP_t)
```

後続作用を受けた実際の自己構造は変化する。

```text
M_1 = Interact(M_0, EFP_{t+Δ})
```

実際の後続解釈：

```text
F = interp(M_1, EFP_{t+Δ})
```

比較用の凍結解釈：

```text
F* = interp(M_0, EFP_{t+Δ})
```

このとき、

```text
E_ext  = Δ(F_hat, F*)
E_self = Δ(F*, F)
E_all  = Δ(F_hat, F)
```

と分けられる可能性がある。

- `E_ext`：自己構造を固定した場合の入力側変化
- `E_self`：相互作用による自己側変化の影響
- `E_all`：実際の総不整合

この整理により、比較可能性と相互作用性を両立できる。

---

## 4. `M_B` より変化量を一次に置く案

議論を進めると、主役は状態 `M_B` そのものではなく、

```text
ΔM_B
```

である可能性がある。

従来：

```text
M_B → F → E → H → Update(M_B)
```

変更候補：

```text
EFP → ΔM_B → M_B'
```

そして `F`、`E`、`H` は、`ΔM_B` の異なる断面・読み取りとして扱う。

例えば、

```text
δM_t = Φ(M_t, EFP_t, ξ_t)
```

を基本運動とする。

そこから、

```text
F_t = I(δM_t, M_t)
E_t = D(δM_t, δM_hat_t)
H_t = Accum(E_t)
```

を派生させる。

このとき予測も、

```text
次に世界がどうなるか
```

ではなく、

```text
次の相互作用によって自己構造がどう変わるか
```

へ寄せられる。

```text
E = Δ(δM_hat_{t+1}, δM_{t+1})
```

この方が、RDLが世界そのものではなく有限境界内の作用・解釈を扱うという前提と整合しやすい。

---

## 5. 初期状態：まず `ξ` しかない

さらに根本へ遡ると、最初から `M_B` があると仮定する必要はない可能性がある。

初期には、

```text
ξ
```

としてしか扱えない未分化・未回収の関係がある。

そこから変化が起きる。

```text
ξ → Δ_1 → Δ_2 → Δ_3 → ...
```

最初は「何があるか」は分からない。

しかし、変化列の中に、

- 反復する差
- 消えにくい差
- 戻りやすい差
- 特定方向へ偏る差
- 他の変化を拘束する差

が現れると、そこから持続的構造を抽出できる。

```text
ξ
→ Δ
→ pattern(Δ)
→ M_B
```

したがって、`M_B` は最初から存在する既知構造ではなく、

> 未知の変化履歴から析出した一時的な整合構造

として理解できる。

---

## 6. 「分からないもの」だけから「分かるもの」が生まれる

ここで重要なのは、

```text
ξ = 無秩序
```

ではないことである。

`ξ` はあくまで、

```text
ξ = 現在の境界では分からない / 回収されていない関係
```

である。

したがって、最初は全てが未分化でも、変化履歴を追うことで、

```text
同じように変わる
繰り返す
戻る
残る
偏る
```

という関係は抽出できる。

「分かる」とは、未知そのものの正体を完全に知ることではなく、

> 未知の変化の中に、再現・予測可能な関係が析出すること

と捉えられる。

このとき、

```text
ξ → M_B + ξ'
```

となる。

未知が既知へ完全変換されるのではなく、未知の中から一時的な既知構造が立ち上がる。

---

## 7. ランダムウォークとの対応

初期状態を簡略化すると、ランダムウォークに近い形で考えられる。

```text
x_{t+1} = x_t + η_t
```

ここでは、まず変化しか観測できない。

```text
Δx_t = η_t
```

完全にランダムであれば、持続的構造は抽出できない。

しかし実際の変化列に、ランダムでは説明しきれない偏りが継続するなら、

```text
persistent deviation from random walk
```

を構造候補として拾うことができる。

したがって、

```text
M_B = ランダムでは説明しきれない変化の持続的偏り
```

という理解が可能である。

ただし、

```text
ξ ≠ random
```

である。

ランダムは `ξ` の定義ではなく、構造未抽出時の零仮説に近い。

---

## 8. 生存バイアス的ランダムウォーク履歴

さらに、変化の全履歴が残るわけではない。

```text
{Δ}_{all}
```

のうち、

- すぐ消えるもの
- 打ち消されるもの
- 破断するもの
- 他の変化に吸収されるもの

は残存しない。

一方、

- 何度も再現される
- 他の変化を拘束する
- 破断しにくい
- 同一条件下で残存しやすい

関係は履歴として厚くなる。

```text
{Δ}_{all}
→ selection
→ {Δ}_{survived}
```

その圧縮が `M_B` になる。

```text
M_B = survived history of relational change
```

あるいは、

> `M_B` は、過去の相互作用の中で消滅せず残った関係履歴の束である。

と表現できる。

---

## 9. 自由度の高い `M_B` が消滅し、安定構造が残る

初期には、多数の構造候補が立ち上がると考えられる。

```text
M_B^(1), M_B^(2), M_B^(3), ...
```

相互作用を受けるたびに、維持できない候補は消滅する。

```text
M_B^(i)
→ interaction
→ ∅
```

一方、自己維持できる候補は残る。

```text
M_B^(j)
→ interaction
→ M_B^(j)'
```

その結果、構造群は概ね、

```text
高自由度・破断しやすい候補
→ 選別
→ より安定した残存構造
```

へ移る。

ただし、これは単純な自由度減少ではない。

より正確には、

> 破断しやすい自由度が削られ、相互拘束された自由度が残る

と考える方がよい。

したがって安定とは、

```text
stability = 特定B・特定条件下での残存性
```

であり、絶対的・終端的安定ではない。

---

## 10. 空気と風：構造化の例

空気は常に存在していても、通常は強く意識されない。

しかし、圧力差・温度差・速度差などが生じると流れが発生し、「風」として構造化される。

重要なのは、空気そのものではなく、

```text
勾配差
```

である。

例えば、

```text
∇P ≠ 0
∇T ≠ 0
```

によって流れが生じる。

人間は「風」を直接単一入力として受けるのではなく、

```text
Δ皮膚
Δ髪
Δ葉
Δ音
```

など複数の変化の相関から、風という構造を切り出す。

```text
{Δ_skin, Δ_hair, Δ_leaf, Δ_sound}
→ correlated gradient pattern
→ M_B(風)
```

この例から、構造形成の起点は単なる変化量より、

```text
勾配差
```

である可能性が高い。

---

## 11. 勾配差を一次量とする案

候補となる基本流れ：

```text
ξ
→ 勾配差
→ 流れ
→ 持続する流れ
→ 構造化
→ M_B
```

より圧縮すると、

```text
ξ
→ ∇
→ flow
→ persistence
→ M_B
```

状態そのものより、状態間の差、さらにその空間的・時間的勾配差が構造を立ち上げる。

したがって、RDLの動作上の最小単位候補として、

```text
関係
```

だけでなく、

```text
関係の勾配差
```

を検討する価値がある。

---

## 12. T1基本代謝との対応

ここまでの議論は、最終的にはT1基本代謝として整理するのが自然である。

既存のT1基本形：

```text
SILN展開
↓
検査と選別
↓
再構成
```

今回の議論を対応させると、

```text
ξ
↓
勾配差・変化候補の発生
↓
候補構造の展開
↓
破断・消滅・残存
↓
残存関係の抽出
↓
再構成
↓
M_B
```

となる。

対応関係：

| T1工程 | 今回の解釈 |
|---|---|
| SILN展開 | 未分化な作用・変化・勾配差から候補構造を展開する |
| 検査と選別 | 相互作用下で破断・消滅する構造と残存する構造を分ける |
| 再構成 | 残存履歴を圧縮し、次の `M_B` として形成する |

したがって、

> `M_B` がどのように形成されるかという問題は、主としてT1の代謝過程として扱う。

という整理が有力である。

---

## 13. T0とT1の責務分担候補

### T0

T0では、基底条件として次を保持する。

```text
B
ξ
有限構造
相互作用
非終端性
```

さらに最低限、

```text
有限構造は固定物ではなく、相互作用によって変化しうる
```

ことだけを保証する。

T0で `M_B` の具体的生成アルゴリズムまで固定しない。

### T1

T1では、

```text
ξ
→ 勾配差
→ 変化候補
→ 選別
→ 残存
→ 再構成
→ M_B
```

という代謝過程を扱う。

したがって役割を圧縮すると、

```text
T0 = M_B + ξ が成立し、有限構造が相互作用で変化しうることを定める
T1 = M_B がどのように析出・残存・更新されるかを扱う
```

となる。

---

## 14. 現行 `F / F' / E / H` の再検討ポイント

今回の整理を採用する場合、現行SPECの以下は再検討対象になる。

### 14.1 `F'`

現行：

```text
F' = interp(同一の更新前M_B, 後続EFP)
```

候補：

- 実際の後続解釈は変化後 `M_B'` から形成する
- 同一 `M_B` の `F*` は比較用仮想量へ降格する

### 14.2 `E`

現行：

```text
E = Δ(F, F')
```

候補：

```text
E = 予測された変化量と実際の変化量の差
```

または、外部差と自己変化差を分離する。

### 14.3 `H`

現行では `H` は誤差蓄積として後段に置かれる。

しかし、`δM_B` を一次量に置く場合、`H` は

```text
変化量のうち、吸収されず残存した成分
```

として再定義した方が自然な可能性がある。

### 14.4 更新

現行：

```text
E → H → θ → Update(M_B)
```

候補：

```text
EFP → δM_B
```

という常時微小変化を基本に置き、

```text
δM_B の蓄積・拘束破断
→ θ
→ 大規模再編
```

とする。

つまり、

```text
常時微小更新
```

と

```text
閾値を越えた構造再編
```

を分離する。

---

## 15. 暫定圧縮モデル

現時点の議論を最短化すると、次の形になる。

```text
ξ
↓
勾配差 ∇
↓
変化 Δ
↓
候補構造群 {M_B}
↓
相互作用
↓
破断・消滅・残存
↓
残存履歴
↓
再構成
↓
M_B'
↓
次の勾配差・変化を拘束
```

循環形：

```text
ξ
→ ∇
→ ΔM
→ Selection
→ M_B
→ biased ΔM
→ Selection
→ M_B'
→ ...
```

より概念的には、

> 未知の中に勾配差が生じる。  
> 勾配差が流れを作る。  
> 流れのうち残存するものが履歴になる。  
> 残存履歴が `M_B` として構造化される。  
> `M_B` は次の変化を拘束し、その拘束下で再び選別が起こる。

---

## 16. 暫定定義候補

### `ξ`

現在の有限境界では回収・構造化されていない関係。

```text
ξ ≠ random
ξ = uncollected / unresolved relation
```

### 勾配差

未分化な関係の中で検出される局所的・時間的な差異の偏り。

```text
∇R
```

### `ΔM_B`

相互作用によって有限構造に生じる変化量。

### `M_B`

候補定義：

> 有限境界 `B` のもとで、相互作用と選別を通過し、現在まで残存している関係履歴の圧縮構造。

別表現：

> `M_B` は、過去の勾配差と変化のうち、破断・消滅せず残った関係が作る一時的な整合慣性である。

---

## 17. 未決事項

以下はまだ確定しない。

1. `ξ` から直接「勾配差」が出ると表現してよいか
2. 勾配差をT0 Primitiveへ昇格させるか、T1操作量に留めるか
3. `M_B` の生成をT1に完全移管するか
4. `F` を独立変数として残すか、`ΔM_B` の観測断面として再定義するか
5. `E` を `F-F'` から `ΔM` の予測誤差へ変更するか
6. `H` と `δM_B` の関係をどう整理するか
7. 常時微小更新と大規模再編を別レベルに分けるか
8. SILN展開と候補 `M_B` の関係をどのように定義するか
9. 「生存」「選別」という語が生物学的意味へ過度に引っ張られないか
10. ランダムウォークを正式モデルにするか、説明用零仮説に留めるか

---

## 18. 現段階の要点

現時点で最も強い変更候補は、次の三点である。

```text
1. M_Bを固定状態ではなく、残存履歴から形成される一時的構造として強く捉える。

2. 相互作用はFを形成するだけでなく、常にδM_Bを生じさせると考える。

3. M_B形成の実過程はT0ではなく、
   ξ → 勾配差 → 変化 → 選別 → 残存 → 再構成
   というT1基本代謝として扱う。
```

この整理を採用する場合、T0はより薄く基底条件へ戻り、T1が「構造が生まれ、残り、変わる」実際の代謝を担う形になる。

