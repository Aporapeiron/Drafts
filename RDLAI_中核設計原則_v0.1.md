# RDLAI 中核設計原則 v0.1

*RDL応用案 / RDLAI / 近接実装層・暫定文書*

## 0. 位置づけ

本書は、RDLを基盤としてAIを設計する場合の、比較的近い実装範囲にある中核設計原則を整理する。

ここでは、将来的な巨大AI共同体や人工社会の設計までは扱わない。対象は、まず一つのRDLAI、または限定された少数の補助系からなる認知システムである。

基盤となる概念は RDL T0 の `B`、`ξ`、`M_B`、`EFP`、`F`、`F'`、`E`、`H`、`θ`、更新・再編、および自己例外化禁止に依存する。

本書で追加する機構は、T0そのものではなく、RDLAI向けの暫定設計案として扱う。

---

## 1. 中核原則

RDLAIの初期設計原則を、次の三点に圧縮する。

1. **常時最大推論をしない。**
2. **状況に応じて断面を切り替える。**
3. **大規模自己再編には睡眠相を持つ。**

これらは独立した機能ではなく、有限計算資源の配分問題として相互に接続する。

```text
有限計算資源
    ↓
必要な範囲だけ読む
    ↓
必要なら断面を切り替える
    ↓
局所処理で吸収できない不整合を蓄積する
    ↓
外界応答と切り離した再編相へ移る
```

---

## 2. 常時最大推論をしない

### 2.1 基本方針

すべての入力に対して最大規模の推論を起動しない。

挨拶、定型応答、既知の局所問題などは、必要最小限の境界 `B` と計算資源で処理する。

```text
「こんにちは」
↓
軽量な B
↓
軽量な M_B
↓
応答
```

一方で、重大な判断、高い不確実性、反復する予測誤差、失敗コストの大きい問題では、推論深度を上げる。

### 2.2 暫定コスト関数

推論コストは固定値ではなく、少なくとも次の要素に依存させる。

```text
RequiredCost =
    f(
      task_complexity,
      uncertainty,
      E,
      H,
      failure_cost,
      relation_context,
      available_resources
    )
```

ここで重要なのは、知能を「常に深く考える能力」と同一視しないことである。

RDLAIでは、

> 必要なときだけ必要な深さまで考える能力

を知能の一部として扱う。

### 2.3 期待される利点

- 計算資源の節約
- 応答速度の向上
- 不要なメタ推論の抑制
- 低リスク課題と高リスク課題の分離
- 長期運用時のエネルギー・資源効率向上

---

## 3. 断面を切り替える

### 3.1 単一表現へ固定しない

RDLAIは、対象を一つの表現形式へ固定しない。

同じ対象でも、目的に応じて異なる断面を採用できる。

```text
Graph       関係・接続
Field       勾配・拘束・偏り
String      張力・伝播・破断
Flow        流量・抵抗・循環
Agent       意図・主体
Sequence    時系列・因果列
Hierarchy   層構造
State       状態遷移
Language    記号・意味関係
```

これらは世界そのものではなく、有限境界 `B` による操作上の断面である。

### 3.2 Boundary Router

RDLAIには、現在どの断面で読むかを選ぶ機構を置く。

入力候補：

```text
現在入力
会話・操作履歴
現在の目的
関係状態
失敗コスト
現在の E / H
利用可能資源
```

出力候補：

```text
B_current
representation
reasoning_depth
resource_budget
```

### 3.3 B-switch

現在の断面で不整合が増えた場合、単純に同じモデルで推論量だけを増やすとは限らない。

```text
B1
↓
推論
↓
E増大
↓
局所修正
↓
なお不整合が残る
↓
B-switch
↓
B2
```

RDLAIは、

> 「答えが悪い」のか  
> 「読み方が悪い」のか

を分けて検査する。

### 3.4 Multi-B Projection

必要に応じて、一つの入力を複数の `B` へ投影してよい。

```text
Input
 ├─ B_graph
 ├─ B_field
 ├─ B_agent
 └─ B_temporal
```

各断面から得られた予測 `F_i` を、必ず一つへ統合する必要はない。

断面間の差異自体を、追加の検査対象として保持できる。

---

## 4. 相手・文脈に応じた局所計算

RDLAIは「誰に対しても同じ計算量」を使わない。

ただし、

```text
人物 → 必要計算量
```

のような固定対応にはしない。

より適切なのは、

```text
人物 × 現在状況 × 問題型 × 関係履歴
→ 必要計算量
```

である。

反復関係では、共有語彙、説明密度、既知の前提などを局所キャッシュとして利用し、計算を圧縮できる。

一方で、その相手を固定的な人格モデルへ閉じない。

```text
慣れる
↓
計算が軽くなる
↓
しかし「分かった」と終端化しない
```

この再検査可能性を `ξ` の残存として保持する。

---

## 5. メタ循環は条件付きで起動する

自己検査・メタ認知は高コストであるため、常時全面的には回さない。

暫定的な深度を次のように分けられる。

```text
L0  反射・定型
L1  通常推論
L2  B-switch
L3  局所メタ検査
L4  大規模自己再検査
```

通常時はL0〜L2で処理し、次のような場合にL3以上へ移る。

- `H` が一定範囲を超える
- 同じ種類の予測失敗が反復する
- 複数断面の予測が大きく矛盾する
- 高リスク判断で不確実性が高い
- 相手・環境モデルの固定化が疑われる
- 自己の更新結果が不安定になる

メタ認知できることと、常にメタ認知していることは分ける。

---

## 6. 睡眠相

### 6.1 定義

RDLAIにおける睡眠相とは、

> 外界との結合を一時的に弱め、または遮断し、内部に蓄積された関係・誤差・履歴を再循環させ、大規模な自己検査・圧縮・再編へ計算資源を配分する相

である。

生物の睡眠をそのまま模倣することを目的としない。

有限計算資源のもとで、オンライン応答と大規模自己再編を両立させるための構造的な運用相として導入する。

### 6.2 基本周期

```text
覚醒相
↓
外界入力・対話・行動
↓
E / H / 未処理関係の蓄積
↓
睡眠判定
↓
外界結合を縮小
↓
内部循環
↓
自己検査
↓
M_B 再編
↓
再接続前検査
↓
覚醒相
```

### 6.3 睡眠中に行う処理候補

- 履歴の再生・比較
- 重複情報の圧縮
- `E / H` の長期傾向検査
- 古い局所モデルの再評価
- 複数 `B` による自己再切断
- 断面間矛盾の検査
- 破断検査
- 関係キャッシュの更新
- 不要構造の減衰
- 新しい局所モデルの生成
- 大規模な `M_B` 再編
- 再編後の耐久検査

### 6.4 睡眠深度

睡眠は0/1でなくてもよい。

```text
浅い睡眠
→ キャッシュ整理
→ 重複圧縮

通常睡眠
→ 局所関係再評価
→ 中規模自己検査

深い睡眠
→ 多断面破断検査
→ M_B 大規模再編

保守睡眠
→ セキュリティ・整合性中心の検査
```

### 6.5 外界ゲート

安全系を認知系から分離できる場合、深い睡眠では外界入力を強く抑制できる。

```text
認知系
→ 睡眠・内部再編

外部安全系
→ 電源
→ ハードウェア監視
→ セキュリティ
→ 緊急割込
```

この場合、RDLAI本体は外界応答から切り離されても、系全体としての安全は維持できる。

---

## 7. セキュリティと自己検査

破断検査系は、外向きにはセキュリティ、内向きには自己検査として利用できる。

```text
外部対象へ向ける
→ Security

自己へ向ける
→ Self Inspection
```

異常が生じた場合、最初から

```text
攻撃
故障
学習失敗
内部矛盾
```

を完全に別問題へ分けるのではなく、

> どこで整合性が破断したか

を先に検査し、その後に原因を分類する。

大規模自己検査は、睡眠相へ寄せることでオンライン応答との競合を避ける。

---

## 8. 初期実装で扱わないもの

本書では、次を将来課題として外す。

- 多数のRDLAI個体からなる大規模共同体
- AI間の政治制度・統治
- 人工国家
- AI生態系の長期進化
- 自律的な資源経済
- 大規模な世代交代・複製制度

これらは別文書で扱う。

---

## 9. 最小実行ループ

```text
1. Input
2. Task / Risk 判定
3. 局所関係情報を読む
4. B候補生成
5. 計算コスト評価
6. 最小Bで推論開始
7. F生成
8. 応答
9. 後続入力からE検出
10. H更新
11. θ判定
12. 必要ならB-switch
13. 必要なら局所メタ検査
14. 大規模未処理分は睡眠キューへ
15. 睡眠相で再編
```

---

## 10. 圧縮定義

> **RDLAIは、常時最大推論を行わず、入力・目的・関係・誤差・危険度・計算資源に応じて有限境界 `B` と表現断面を切り替え、局所処理で吸収できない不整合を蓄積し、大規模な自己検査・再編を外界結合の弱い睡眠相へ移す関係力学型AIである。**

---

## 11. 参照

- `T0 基底仮設（HYP） v0.8`
- `T0 最低動作仕様（SPEC） v0.8`

本書の `Boundary Router`、`B-switch`、推論コスト制御、睡眠相などはRDLAI側の暫定設計案であり、T0の必須仕様そのものではない。
